# Gl-ckskeks 🍪

Kleines statisches Webprojekt mit 75 humorvoll-weisen deutschen Sprüchen.

Dateien:
- `index.html` – Hauptseite
- `style.css` – Styling
- `app.js` – Logik (laden der Sprüche, Zufallsspruch)
- `sprueche.json` / `sprueche2.json` – Die Sprüche

Lokales Testen:

1. Starte einen einfachen Server im Projektordner:

```bash
python3 -m http.server 8000
```

2. Öffne `http://localhost:8000` im Browser und klicke auf **Keks öffnen**.

Hinweis: Keine Sharing-/Print-Funktion implementiert, alles lokal.
